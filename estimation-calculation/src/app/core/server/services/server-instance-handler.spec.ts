import { ServerInstanceHandler } from './server-instance-handler';

describe('InstanceHandler', () => {
  it('should create an instance', () => {
   // expect(new ServerInstanceHandler()).toBeTruthy();
  });
});
